#include <stdio.h>

/**
 * main - Entry point Return: Always 0 (Success)
 * Return: 0 if exited properly,else return non zero
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
